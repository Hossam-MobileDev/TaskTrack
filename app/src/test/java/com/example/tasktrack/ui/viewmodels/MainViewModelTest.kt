package com.example.tasktrack.ui.viewmodels

import com.example.tasktrack.repository.FakeMainRepository
import org.junit.Assert.*
import org.junit.Before
import org.junit.Test

class MainViewModelTest{


    private lateinit var viewModel: MainViewModel

    @Before
    fun setup() {
        //viewModel = MainViewModel(FakeMainRepository())
    }
    @Test
    fun `insert data`() {
        //val value = viewModel.insertTrack(track ).getOrAwaitValueTest()
    }
}